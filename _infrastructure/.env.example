# ==========================================
# PostgreSQL Configuration
# ==========================================
POSTGRES_DB=orange_db
POSTGRES_USER=mikasa
POSTGRES_PASSWORD=mikasa

# ==========================================
# WAHA WhatsApp Configuration
# ==========================================

# Generate a strong API key (use: uuidgen | tr -d '-')
# Or use SHA512 hash: echo -n "your-key" | shasum -a 512
WAHA_API_KEY=your-secret-api-key-here

# Dashboard credentials (CHANGE IN PRODUCTION!)
WAHA_DASHBOARD_USERNAME=admin
WAHA_DASHBOARD_PASSWORD=change-me-in-production

# Webhook HMAC secret for signature verification
WAHA_WEBHOOK_HMAC_SECRET=your-webhook-hmac-secret

# ==========================================
# Spring Boot Configuration
# ==========================================
SPRING_PROFILES_ACTIVE=dev

# ==========================================
# Security Notes
# ==========================================
# 1. Generate strong random values for all secrets:
#    - API Key: uuidgen | tr -d '-'
#    - HMAC Secret: openssl rand -hex 32
# 2. Never commit actual secrets to git
# 3. Use environment-specific .env files (.env.dev, .env.prod)
# 4. Rotate secrets regularly in production
# 5. Store production secrets in secure vault (AWS Secrets Manager, HashiCorp Vault)
