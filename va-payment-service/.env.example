# Mock Payment Service Configuration

# BNI API Key for calling transaction-service (must match API Gateway config)
BNI_API_KEY=bni-api-key-dev

# Transaction Service Base URL (via API Gateway)
TRANSACTION_SERVICE_BASE_URL=http://localhost:8080

# Webhook Secret for signature calculation (must match Transaction Service config)
# This must match: orange.integration.bni-va.client-secret in transaction-service
WEBHOOK_SECRET=secret-key-for-dev

# Server Port
PORT=8090
