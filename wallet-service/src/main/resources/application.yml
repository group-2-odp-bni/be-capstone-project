spring:
  config:
    import: optional:file:.env[.properties] # 👈 ini yang memuat file .env di root wallet-service
  application:
    name: wallet-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false
